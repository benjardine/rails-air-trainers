
<div class="index-container border border-primary">
  <div class="cards-container border border-secondary">
    <h2 class="section-title">Training next to you</h2>
    <%= link_to "+Add a New Session", new_training_session_path %>
    <%= render "shared/card", t_sessions: @t_sessions %>
  </div>
  <div class="map border border-secondary">
  <div id="map"
     style="width: 100%; height: 600px;"
     data-markers="<%= @markers.to_json %>"
     data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"></div>
  </div>
</div>

